# Keep rules for the example application only. The Mosaic SDK contributes none: see
# `sdk/android/mosaic/consumer-rules.pro`.
#
# This file exists so `:app:assembleRelease` runs R8 with an explicit, reviewable configuration. It
# is intentionally empty apart from this note; if a rule ever becomes necessary here, it must be
# justified as an example-application need, never as a hidden SDK requirement.
