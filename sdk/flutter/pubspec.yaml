name: mosaic_sdk
description: Native Mosaic Protocol 0.1 and 0.2 renderer with Local Preview for Flutter.
version: 0.2.0-dev.2
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
