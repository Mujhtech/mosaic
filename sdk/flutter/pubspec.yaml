name: mosaic_sdk
description: Protocol 0.1 RC1 native paywall renderer and mock commerce for Flutter.
version: 0.1.0-dev.2
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
