{
  "contract": "Placement Decision v1",
  "description": "Generated by tools/generate-rejection-layers.mjs. For each invalid fixture, the layer that rejects it: \"schema\" means the canonical JSON Schema alone rejects it; \"semantic\" means the schema accepts it and a semantic validator rule rejects it. See docs/protocol/fixture-lifecycle.md.",
  "layers": {
    "duplicate-priority.json": "semantic",
    "fallback-cycle.json": "semantic",
    "incompatible-source-operator.json": "semantic",
    "invalid-condition-type.json": "schema",
    "missing-unavailable-fallback.json": "semantic",
    "overdeclared-features.json": "semantic",
    "qa-override-over-24h.json": "semantic",
    "underdeclared-features.json": "semantic",
    "unsupported-operator.json": "schema"
  }
}
